export class Repository {
    constructor(public name:string, public fullname:string, public description:string, public url:string, public created_at:string,
        public clone_url:string,public language:string){

    }
}
