import { HooverDirective } from './hoover.directive';

describe('HooverDirective', () => {
  it('should create an instance', () => {
    const directive = new HooverDirective(8);
    expect(directive).toBeTruthy();
  });
});
